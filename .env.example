# Formspree form ID for the contact form. The default ("xwvgkeda") is baked
# into lib/data.ts; set this only to override it at build time, e.g. to point
# staging at a different form.
NEXT_PUBLIC_FORMSPREE_ID=
